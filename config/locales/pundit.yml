##
# As its an error based file, all messages should say the OPPOSITE of the condition.
#
---
en:
  pundit:
    errors:
      record_belongs_to_user?: "Record does not belong to user"
      records_belongs_to_user?: "Records do not belong to user"
      user_owns_notes?: "User does not own selected notes"
